{
  "name": "tradefourge-extension",
  "version": "3.1.0",
  "private": true,
  "description": "TradeFourge Companion Extension v3.1 — Production Bundled Extension Bridge",
  "scripts": {
    "build": "npx esbuild content.js --bundle --outfile=dist/content.js --format=iife && npx esbuild background.js --bundle --outfile=dist/background.js --format=iife"
  }
}
